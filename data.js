class data {

}

// send the data to the backend file, parsing the data from the front end, req.body
// making sure the database allows inserting data
// we need an id inside of database, which is an INT which increases, auto increment